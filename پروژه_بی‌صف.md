# مستند جامع مهندسی معکوس پروژه بی‌صف (BiSaf Project Blueprint)

این مستند شامل تمام اطلاعات معماری، دیتابیس، ساختار فایل‌ها، طراحی UI/UX، سرویس‌ها و منطق‌های سیستم "بی‌صف" است و به عنوان یک راهنمای کامل (Blueprint) برای بازسازی یا توسعه این پلتفرم توسط AI Agent کاربرد دارد.

## ۱. معرفی و هدف پروژه
**بی‌صف (BiSaf)** یک پلتفرم هوشمند مدیریت صف و نوبت‌دهی آنلاین است که به مشتریان اجازه می‌دهد بدون نیاز به حضور فیزیکی در صف‌ها (مثل نانوایی، آرایشگاه، پزشک و...) نوبت بگیرند، زمان انتظار را به صورت زنده پیگیری کنند و از طریق پیامک یا ربات‌های پیام‌رسان (بله و تلگرام) اعلان دریافت کنند. هدف اصلی: صرفه‌جویی در زمان، کاهش ازدحام فیزیکی و ارائه یک سیستم مدیریت یکپارچه برای متصدیان فروشگاه، بازاریابان و مدیران کل.

## ۲. تکنولوژی‌ها و معماری فنی
*   **فرانت‌اند:** React 18, TypeScript, Vite, Tailwind CSS (Mobile-first, پشتیبانی کامل از Dark/Light Mode)
*   **بک‌اند و دیتابیس:** Supabase (PostgreSQL, RLS, Edge Functions)
*   **مدیریت وضعیت (State):** React Hooks (useState, useEffect, Context API), `StorageService` (جهت انتزاع داده‌های Mock/Local/DB).
*   **آیکون‌ها:** `lucide-react`
*   **نقشه:** `leaflet`, `react-leaflet`, با استفاده از Neshan Maps API
*   **چاپ و بارکد:** `jspdf`, `html2canvas`, `qrcode`
*   **معماری فایل‌ها (File Structure):**
    *   `/src/pages/`: صفحات اصلی سیستم (Landing, Login, Dashboard, Panels).
    *   `/src/components/`: کامپوننت‌های UI مجددپذیر (Navbar, BottomNav, Modals, Forms).
    *   `/src/services/`: منطق سرویس‌ها (AuthService, StorageService, BotService, MockData).
    *   `/src/utils/`: ابزارهای کمکی (مانند تبدیل تاریخ جلالی و مدیریت خطاها).
    *   `/src/types.ts`: تعریف مدل‌های داده و TypeScript Interfaces.
    *   `/supabase/migrations/`: اسکریپت‌های کامل SQL برای ایجاد جداول دیتابیس.
    *   `/supabase/functions/bot-webhook/`: توابع Edge بک‌اند برای مدیریت یکپارچه ربات‌ها.
    *   `project_management.md`: نگاشت دقیق وضعیت توسعه و لیست تسک‌ها.

## ۳. صفحات، مسیرها، کامپوننت‌ها و UI/UX
*   **`/` (LandingPage):** صفحه معرفی پروژه با طراحی دعوت‌کننده و داستان‌گویی. دارای Smart CTA برای هدایت مشتری به `/dashboard`، مهمان به `/login` و مخفی کردن CTA برای نقش‌های غیرمشتری.
*   **`/login` (LoginPage):** فرم ورود و ثبت‌نام یکپارچه. دارای دو تب: OTP پیامکی (و ثبت‌نام خودکار در صورت عدم وجود حساب) و ورود با نام‌کاربری/رمزعبور. **مهم:** بالاترین بخش به دکمه‌های ورود سریع با ربات بله و تلگرام به عنوان جذاب‌ترین و سریع‌ترین مسیر اختصاص دارد.
*   **`/dashboard` (CustomerDashboard):** داشبورد مشتری. شامل نقشه‌یاب زنده (Neshan)، کارت‌های نوبت‌های فعال، نوبت‌های پیشین، فروشگاه‌های محبوب و پروفایل.
*   **`/shop/:slug` (PublicShopPage):** صفحه اختصاصی هر فروشگاه برای دریافت نوبت توسط مشتری. امکان انتخاب نوع و تعداد محصول/خدمت، نمایش زنده صف و صدور فیش PDF یا QR Code.
*   **`/panel/:id` (ShopkeeperPanel):** پنل متصدی. مدیریت زنده صف، فراخوانی نوبت بعدی (همراه با سیستم صوتی TTS)، تایید تحویل، مدیریت محصولات و گزارش‌گیری.
*   **`/marketer` (MarketerDashboard):** پنل بازاریاب. ثبت فروشگاه جدید با لوکیشن و پیگیری وضعیت مالی و پورسانت‌ها.
*   **`/admin` (AdminDashboard):** پنل مدیر کل. آمار کلان سیستم، مدیریت کاربران، تایید فروشگاه‌ها و بازاریابان، تنظیمات سراسری (Feature Toggles).

**کامپوننت‌های کلیدی UI:**
*   **`Navbar`:** هدر اصلی، نمایش وضعیت کاربر فعال، تغییر پوسته (Dark/Light)، ساعت زنده جلالی، خروج هوشمند.
*   **`BottomNav`:** منوی پایین هوشمند و کاملاً داینامیک. براساس نقش (مهمان، مشتری، متصدی، بازاریاب، ادمین) تب‌های متفاوتی رندر می‌کند. برای مهمانان، گزینه "عضویت سریع" یک Bottom Sheet باز می‌کند که دموهای تعاملی از امکانات سامانه (پیگیری زنده، تاریخچه، هشدار نوبت) را نمایش داده و وی را به سمت ربات بله/تلگرام یا ثبت‌نام با موبایل هدایت می‌کند.

## ۴. احراز هویت، OTP و نقش‌های کاربری
*   **نقش‌ها (Roles):** `guest` (مهمان)، `customer` (مشتری)، `shopkeeper` (متصدی)، `marketer` (بازاریاب)، `admin` (مدیر).
*   **روند احراز هویت (Unified Standard Flow):** 
    1.  **Bot-First Registration (اولویت اول):** هدایت به ربات با لینک‌های Deep Link (`/start guest` یا `register`). پس از ارسال شماره در پیام‌رسان، کاربر سریعاً ساخته شده و لاگین می‌شود.
    2.  **SMS OTP / Auto-Register:** ورود شماره موبایل -> دریافت و تایید کد ۵ رقمی -> لاگین. اگر اکانت وجود نداشت، به صورت خودکار با نقش `customer` ثبت‌نام می‌شود.
    3.  **Password:** روش کلاسیک ورود با شماره موبایل یا نام کاربری به همراه رمز عبور.
*   **مدل داده کاربر (`Profile`):** شامل `id`, `phone`, `username`, `full_name`, `password` (Hashed در بک‌اند), و `role`.
*   **قانون یکپارچگی:** تمام مسیرهای ورود به یک پایگاه داده متصل می‌شوند. هیچ سیستم موازی برای ثبت‌نام وجود ندارد.

## ۵. دیتابیس (Supabase PostgreSQL Schema)
دیتابیس در فایل‌های Migration به دقت طراحی شده و شامل جداول زیر است:
*   **`profiles`:** اطلاعات کاربران و نقش‌ها.
*   **`shops`:** جزئیات فروشگاه‌ها، آدرس، مختصات جغرافیایی، دسته‌بندی و تنظیمات خدمت‌دهی.
*   **`queue_items`:** هسته نوبت‌دهی. ثبت وضعیت نوبت (`waiting`, `serving`, `served`, `cancelled`)، زمان‌های کلیدی و سفارشات.
*   **`marketers` / `marketer_shops`:** سیستم مدیریت بازاریابان، نرخ پورسانت و وابستگی فروشگاه‌ها به آن‌ها.
*   **`user_bot_mappings`:** پیوند دهنده `chat_id` شبکه‌های اجتماعی (بله/تلگرام) به `user_id` برای Seamless Auth.
*   **`bot_dynamic_texts`:** متون داینامیک برای مدیریت پیام‌های ربات‌ها (پیام خوشامدگویی به تفکیک نقش) قابل ویرایش از پنل ادمین.
*   **روابط (Relations):** تمامی جداول با Foreign Keyهای منطقی (Cascade On Delete/Set Null) به هم متصل هستند.
*   **توابع RPC:** تابع قدرتمند `serve_next_queue_item` به زبان PL/pgSQL برای پردازش منطق پیچیده نوبت‌دهی و اولویت‌بندی افراد تکی/چندتایی (Interleaving) مستقیماً درون دیتابیس طراحی شده است.

## ۶. سیستم صف، QR Code و محاسبه زمان انتظار
*   مشتری هنگام گرفتن نوبت، در جدول `queue_items` رکورد ایجاد می‌کند. سیستم بر اساس `avg_service_time` (میانگین زمان خدمت‌دهی تنظیم شده در فروشگاه) و تعداد افراد `waiting` قبل از وی، زمان تخمینی را محاسبه و در رابط کاربری نشان می‌دهد.
*   کد نوبت به صورت بارکد (QR Code) در فیش مجازی تولید می‌شود تا متصدی بتواند آن را اسکن کند (توسط `qrcode` module).
*   وضعیت صف توسط متصدی از طریق کنترل‌های پنل و توابع `serve` تغییر می‌کند و رویدادهای صوتی فراخوانی را در محل فروشگاه تریگر می‌کند.

## ۷. یکپارچگی ربات‌های پیام‌رسان (بله و تلگرام)
*   **معماری بک‌اند:** یک تابع Edge در Supabase (`bot-webhook/index.ts`) با معماری Adapter Pattern نوشته شده که درخواست‌های وب‌هوک هر دو پلتفرم تلگرام و بله (به دلیل ساختار API مشابه) را به صورت متمرکز هندل می‌کند.
*   **User Flow داخل ربات (Webhook Logic):**
    1.  کاربر دکمه Start را می‌زند. وب‌هوک `chat_id` را دریافت می‌کند.
    2.  در جدول `user_bot_mappings` جستجو می‌شود.
    3.  **کاربر لاگین نشده (مهمان):** متن جذابِ `guest_welcome` (خوانده شده از `bot_dynamic_texts`) به همراه دکمه `request_contact` (تایید شماره) ارسال می‌شود. به محض ارسال Contact، کاربر در دیتابیس ثبت‌نام/یافت شده و Mapping برقرار می‌شود.
    4.  **کاربر لاگین شده:** بر اساس نقش وی (`role`)، پیام خوشامدگویی اختصاصی (`customer_welcome`, `shopkeeper_welcome` و ...) و کیبوردهای (Reply Markup) مخصوص نقش برای او ارسال می‌شود.
*   **دکمه‌ها و منوهای نمونه:**
    *   **مشتری:** 📍 پیدا کردن فروشگاه اطراف، 🕒 وضعیت نوبت من، 🎁 دعوت از دوستان.
    *   **متصدی:** 📋 مشاهده صف زنده، ✅ تحویل به نفر بعدی (صدا زدن).

## ۸. داده‌های آزمایشی (Mock Data)
به منظور تست رابط کاربری پیش از راه‌اندازی کامل دیتابیس زنده، سیستم به سیستم داده‌های آزمایشی غنی در `mockData.ts` مجهز است:
*   حساب‌های پیش‌فرض برای تست (مثلاً مشتری `09121234567`، متصدی `09127654321`، و ادمین `09999999999`). رمز عبور عموماً `123456` فرض می‌شود.
*   لیست ده‌ها فروشگاه در رسته‌های نانوایی، آرایشگاه، پزشک و... با لوکیشن‌های تستی (عموماً در تهران).
*   نوبت‌های آزمایشی پر شده برای مشاهده وضعیت صف و داشبوردها.
*   تراکنش‌ها و آمار بازاریابی در جداول شبیه‌سازی شده.
دسترسی به این داده‌ها کاملاً توسط سرویس واسط `StorageService.ts` انتزاع (Abstract) شده است.

## ۹. جریان داده‌ها (Data Flow) و ارتباطات
*   **UI (React Views) <--> Business Logic (`AuthService`, `BotService`, `StorageService`) <--> Data Layer (`mockData` or `Supabase Client`).**
*   **وابستگی منوی BottomNav:** به `StorageService.getCurrentUser()` وابسته است تا نقش کاربر و مسیر فعلی `useLocation` را دریافت کند و رندر هوشمند انجام دهد. تب‌های BottomNav اکشن‌های مختلفی مثل Navigate یا باز کردن Modal را شلیک می‌کنند.
*   **وابستگی ربات‌ها:** فایل‌های Webhook وابسته به دیتابیس Supabase و جداول `bot_dynamic_texts` (با لایه Caching 5 دقیقه‌ای در مموری سرورलेस) و `user_bot_mappings` می‌باشند.
*   **ورود و ثبت‌نام:** `LoginPage` به `AuthService` (برای درخواست و تایید OTP) و `BotService.getBotDeepLink` (جهت هدایت کاربر به بله/تلگرام) وابستگی دارد.

## ۱۰. قواعد توسعه و تصمیمات مهم
*   **Single Source of Truth برای احراز هویت:** پیاده‌سازی سیستم‌های ورود موازی ممنوع است. چه کاربر از پنل لاگین کند، چه از طریق ربات شماره را بفرستد، در نهایت رکورد کاربر در جدول `profiles` قرار می‌گیرد.
*   **Mobile-First Design:** با توجه به اینکه عمده مشتریان با موبایل نوبت می‌گیرند، استفاده از BottomNav، Bottom Sheets و سایزبندی بهینه لمس برای المان‌ها الزامی است.
*   **داینامیک بودن متن ربات‌ها (No Hardcoding):** تمامی دیالوگ‌های طولانی ربات باید در `bot_dynamic_texts` باشند تا مدیر سایت بدون دست‌کاری کد بتواند از روی پنل آن‌ها را تغییر دهد.

## ۱۱. وضعیت تکمیل قابلیت‌ها
*   [x] طراحی UI تمامی صفحات و داشبوردها.
*   [x] معماری دیتابیس کامل (Supabase SQL) و جداول ربات.
*   [x] راه‌اندازی منطق مسیریابی داینامیک، Smart CTA در صفحه فرود و BottomNav.
*   [x] توسعه جریان لاگین استاندارد (OTP/Password + معرفی قدرتمند ربات‌ها).
*   [x] توسعه منطق وب‌هوک ربات تلگرام و بله به همراه کش و Adapter Pattern در Deno Edge Functions.
*   [ ] اتصال نهایی سرویس `AuthService` و `StorageService` به کلاینت Supabase واقعی (در حال حاضر روی Mock Data تنظیم است).
*   [ ] یکپارچه‌سازی درگاه پیامک برای ارسال OTP زنده (در حال حاضر Simulated در لایه لاگین انجام شده).

---
*این فایل آخرین تصویر ساختاری پروژه بی‌صف است و یک هوش مصنوعی قادر است با استفاده از توصیفات بالا، معماری، جداول، منطق و صفحات اپلیکیشن را موبه‌مو بازسازی کند.*
